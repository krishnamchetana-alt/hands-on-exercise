# ReactJS-HOL 2: Shopping Cart

A React shopping-cart hands-on project demonstrating `useState`, props, reusable UI, rendering arrays, conditional UI, and event handlers.

Run:

```powershell
npm.cmd install
npm.cmd run dev
```
