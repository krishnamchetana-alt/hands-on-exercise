# ReactJS-HOL 4: Workout Planner

React project demonstrating stateful activity completion, a controlled daily goal input, and derived calculations.

```powershell
npm.cmd install
npm.cmd run dev
```
