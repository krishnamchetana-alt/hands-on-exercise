# ReactJS-HOL 3: Notes Dashboard

A React notes dashboard showcasing controlled forms, `useState`, `useMemo`, searching, tag filters, rendering lists, and deletion.

```powershell
npm.cmd install
npm.cmd run dev
```
