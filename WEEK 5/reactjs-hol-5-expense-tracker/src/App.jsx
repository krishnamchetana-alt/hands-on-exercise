import {useMemo,useState} from 'react';
const seed=[{id:1,name:'Monthly salary',type:'Income',amount:55000,icon:'✦'},{id:2,name:'Groceries',type:'Food',amount:-1840,icon:'🛒'},{id:3,name:'Internet bill',type:'Bills',amount:-799,icon:'◉'},{id:4,name:'Movie night',type:'Fun',amount:-450,icon:'◌'}];
const currency=n=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(n);
export default function App(){const [transactions,setTransactions]=useState(seed);const [filter,setFilter]=useState('All');const [form,setForm]=useState({name:'',amount:'',type:'Food'});const balance=transactions.reduce((s,x)=>s+x.amount,0);const income=transactions.filter(x=>x.amount>0).reduce((s,x)=>s+x.amount,0);const expenses=Math.abs(transactions.filter(x=>x.amount<0).reduce((s,x)=>s+x.amount,0));const visible=useMemo(()=>transactions.filter(x=>filter==='All'||(filter==='Income'?x.amount>0:x.type===filter)),[transactions,filter]);function add(e){e.preventDefault();const amount=Number(form.amount);if(!form.name.trim()||!amount)return;const signed=form.type==='Income'?Math.abs(amount):-Math.abs(amount);setTransactions(xs=>[{id:Date.now(),name:form.name.trim(),type:form.type,amount:signed,icon:form.type==='Income'?'✦':'●'},...xs]);setForm({name:'',amount:'',type:'Food'});}return <main><header><div className="brand">penny<span>.</span></div><p>YOUR PERSONAL FINANCE SPACE</p><div className="profile">V</div></header><section className="welcome"><div><span className="eyebrow">React 5 · Hands-on</span><h1>Money, made<br/><em>clear.</em></h1><p>Practise React forms, calculated values, filtering and list rendering in a simple expense tracker.</p></div><div className="balance"><small>CURRENT BALANCE</small><b>{currency(balance)}</b><div><span>↑ Income <strong>{currency(income)}</strong></span><span>↓ Spent <strong>{currency(expenses)}</strong></span></div></div></section><section className="summary"><article><span>↗</span><div><small>This month’s income</small><b>{currency(income)}</b></div></article><article><span>↘</span><div><small>This month’s spending</small><b>{currency(expenses)}</b></div></article><article><span>◔</span><div><small>Transactions</small><b>{transactions.length}</b></div></article></section><div className="grid"><section><div className="title"><div><span className="eyebrow">Recent activity</span><h2>Transactions</h2></div><div className="filters">{['All','Income','Food','Bills','Fun'].map(x=><button className={filter===x?'active':''} onClick={()=>setFilter(x)} key={x}>{x}</button>)}</div></div><div className="transactions">{visible.map(x=><article key={x.id}><div className="tx-icon">{x.icon}</div><div><b>{x.name}</b><small>{x.type}</small></div><strong className={x.amount>0?'positive':''}>{x.amount>0?'+':'-'}{currency(Math.abs(x.amount))}</strong></article>)}{!visible.length&&<p className="empty">No transactions in this category.</p>}</div></section><aside><span className="eyebrow">Quick add</span><h2>New transaction</h2><form onSubmit={add}><label>Description<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Coffee"/></label><label>Amount<input type="number" min="1" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})} placeholder="0"/></label><label>Category<select value={form.type} onChange={e=>setForm({...form,type:e.target.value})}><option>Food</option><option>Bills</option><option>Fun</option><option>Shopping</option><option>Income</option></select></label><button type="submit">Add transaction</button></form></aside></div></main>}
