# ReactJS-HOL 12: Trip Planner

React itinerary project demonstrating nested state updates, tab selection, activity filters, and calculated plan totals.

```powershell
npm.cmd install
npm.cmd run dev
```
