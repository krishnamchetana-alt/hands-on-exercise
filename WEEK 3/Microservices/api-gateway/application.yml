server:
  port: 8080

spring:
  cloud:
    gateway:
      routes:
        - id: account_route
          uri: http://localhost:8081
          predicates:
            - Path=/account/**
        - id: loan_route
          uri: http://localhost:8082
          predicates:
            - Path=/loan/**